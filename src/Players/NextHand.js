import React from "react"
import "../styles.css"
import {Clear2} from "../index"
var ReactDOM = require('react-dom');


export function NextHand(){
    return(
        <button className={"cal"} type="button" onClick={Clear2}>Next hand</button>
    )
}
